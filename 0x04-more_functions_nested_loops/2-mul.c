#include "main.h"
/**
 * mul - Entry of the function that multiplies two integers
 * @a: First integer
 * @b: Second integer
 * Return: Return result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
